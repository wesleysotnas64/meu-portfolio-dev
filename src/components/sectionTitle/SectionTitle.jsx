import { MainDiv, Title } from "./SectionTitle.style";

function SectionTitle({title}) {
    return(
        <MainDiv>
            <Title>{title}</Title>
        </MainDiv>

    )
}

export default SectionTitle